name: Build and publish Docker image to GHCR

on:
  push:
    branches:
      - main
      - make-k8s-pipeline-v1
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/skerter/distributed-churn-prediction

jobs:
  build-and-push:
    name: Build and push image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image metadata
        id: meta
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION_TAG="${GITHUB_REF_NAME}"
          else
            SAFE_BRANCH="${GITHUB_REF_NAME//\//-}"
            VERSION_TAG="${SAFE_BRANCH}-${SHORT_SHA}"
          fi

          echo "version_tag=${VERSION_TAG}" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io \
            -u "${{ github.actor }}" \
            --password-stdin

      - name: Build image
        run: |
          docker build \
            -t "${IMAGE_NAME}:${{ steps.meta.outputs.version_tag }}" \
            -t "${IMAGE_NAME}:latest" \
            .

      - name: Smoke test image
        run: |
          docker run --rm "${IMAGE_NAME}:${{ steps.meta.outputs.version_tag }}" \
            python -c "import dask, distributed, pandas, pyarrow, xgboost, coloredlogs, kagglehub; print('ghcr image ok')"

      - name: Push version tag
        run: |
          docker push "${IMAGE_NAME}:${{ steps.meta.outputs.version_tag }}"

      - name: Push latest for main only
        if: github.ref == 'refs/heads/main'
        run: |
          docker push "${IMAGE_NAME}:latest"

      - name: Print image
        run: |
          echo "Pushed image:"
          echo "${IMAGE_NAME}:${{ steps.meta.outputs.version_tag }}"
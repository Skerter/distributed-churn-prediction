"""Константы текстовых сообщений Telegram-бота.

Все строки, отправляемые ботом пользователю, хранятся здесь —
это упрощает локализацию и поиск текстов без погружения в логику хендлеров.
"""

# ---------------------------------------------------------------------------
# Системные / инфраструктурные
# ---------------------------------------------------------------------------

MSG_UNAUTHORIZED = (
    "У вас нет доступа к этому боту.\n"
    "Обратитесь к администратору."
)

MSG_NOT_IMPLEMENTED = (
    "Эта команда ещё не реализована.\n"
    "Следите за обновлениями."
)

# ---------------------------------------------------------------------------
# /start, /help
# ---------------------------------------------------------------------------

MSG_WELCOME = (
    "Привет! Я бот управления пайплайном churn-prediction.\n\n"
    "Используй /help чтобы увидеть список доступных команд."
)

MSG_HELP = (
    "<b>Доступные команды:</b>\n\n"
    "/health — проверить состояние приложения\n"
    "/profiles — список доступных профилей выполнения\n"
    "/run — запустить пайплайн (выбор профиля)\n"
    "/status — проверить статус запуска по run_id\n"
    "/model — информация об обученной модели\n"
    "/cancel — отменить текущий диалог\n"
    "/help — показать это сообщение"
)

# ---------------------------------------------------------------------------
# /health
# ---------------------------------------------------------------------------

MSG_HEALTH_OK = (
    "<b>Приложение работает нормально</b>\n\n"
    "Приложение: {app_name} {app_version}\n"
    "Профиль: {profile_mode}\n"
    "Бэкенд: {backend}"
)

MSG_HEALTH_ERROR = "Ошибка при проверке состояния: {error}"

# ---------------------------------------------------------------------------
# /profiles
# ---------------------------------------------------------------------------

MSG_PROFILES_LIST = (
    "<b>Доступные профили:</b>\n\n"
    "{profiles}"
)

# ---------------------------------------------------------------------------
# /run — выбор профиля и запуск
# ---------------------------------------------------------------------------

MSG_SELECT_PROFILE = "Выберите профиль выполнения пайплайна:"

MSG_PIPELINE_STARTED = (
    "Пайплайн запущен в фоне.\n"
    "run_id: <code>{run_id}</code>\n\n"
    "Используй /status чтобы проверить статус выполнения."
)

MSG_PIPELINE_BUSY = (
    "Уже есть активный pipeline run.\n"
    "Дождись завершения или проверь статус через /status."
)

MSG_PIPELINE_ERROR = "Не удалось запустить пайплайн: {error}"

# ---------------------------------------------------------------------------
# /status
# ---------------------------------------------------------------------------

MSG_STATUS_TEMPLATE = (
    "Статус запуска <code>{run_id}</code>:\n"
    "Состояние: <b>{status}</b>\n"
    "Профиль: {profile}\n"
    "Начало: {started_at}\n"
    "Конец: {finished_at}"
)

MSG_STATUS_NOT_FOUND = "Запуск с run_id <code>{run_id}</code> не найден."

MSG_ASK_RUN_ID = (
    "Введите <b>run_id</b> запуска, статус которого хотите проверить.\n\n"
    "Отмена: /cancel"
)

MSG_STATUS_CANCELLED = "Отменено."

# ---------------------------------------------------------------------------
# /model
# ---------------------------------------------------------------------------

MSG_MODEL_INFO = (
    "<b>Модель:</b> {name} v{version}\n"
    "Режим: {mode}\n"
    "Бэкенд: {backend}\n"
    "Файл: <code>{filename}</code> — {exists}\n\n"
    "<b>Метрики:</b>\n{metrics}"
)
